# Dependencies
/node_modules

# Build output
/dist
*.tsbuildinfo

# Logs
*.log
.tmp/
tmp_kg/

# IDE files
.vscode/
.idea/
.DS_Store
*.swp

# Local environment files
.env
.env.local
.env.*.local

# User-specific runtime config — copy config.example.json to config.json
config.json

# Default outputDir (graph + sqlite + manifests) when config.json doesn't override it
.kg-output/

# Local Claude Code settings (per-machine permission grants)
.claude/settings.local.json
